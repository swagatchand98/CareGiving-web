import React from 'react';
import ProviderRegisterForm from '@/components/auth/ProviderRegisterForm';

export const metadata = {
  title: 'Provider Registration | Caregiving',
  description: 'Register as a service provider on our caregiving platform',
};

export default function ProviderRegisterPage() {
  return <ProviderRegisterForm />;
}
